# URL of the Nova Assist AI Express backend (see server/.env.example).
# Required for real AI chat, chat persistence, and payments - without it the
# app runs in a local-only demo mode with no server sync.
VITE_API_URL=http://localhost:5000
